name: Publish to PSGallery

# Reusable workflow - called from release.yml after the tag is created.
on:
  workflow_call:
    inputs:
      module-path:
        description: >
          Root directory to search for a .psd1 manifest. Defaults to the
          repository root.
        required: false
        type: string
        default: '.'
    secrets:
      PSGALLERY_API_KEY:
        required: true
  workflow_dispatch:
    inputs:
      module-path:
        description: >
          Root directory to search for a .psd1 manifest. Defaults to the
          repository root.
        required: false
        type: string
        default: '.'

jobs:
  run:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5.0.1

      - uses: VitaliiAndreev/GitHub-Common/.github/actions/assert-secret@master
        with:
          value: ${{ secrets.PSGALLERY_API_KEY }}
          name: PSGALLERY_API_KEY

      - uses: ./.github/actions/publish
        if: github.repository == 'VitaliiAndreev/PowerShell-Common'
        with:
          module-path: ${{ inputs.module-path }}
          api-key: ${{ secrets.PSGALLERY_API_KEY }}

      - uses: VitaliiAndreev/PowerShell-Common/.github/actions/publish@master
        if: github.repository != 'VitaliiAndreev/PowerShell-Common'
        with:
          module-path: ${{ inputs.module-path }}
          api-key: ${{ secrets.PSGALLERY_API_KEY }}
